{
    "name": "remove-if-prefix",
    "version": "1.0.1",
    "description": "Remove an item from an object or array of objects if the key starts with a specific character.",
    "repository": "https://github.com/Richienb/remove-if-prefix.git",
    "author": "Richie Bendall <richiebendall@gmail.com>",
    "license": "MIT",
    "main": "index.js",
    "files": [
        "index.js",
        "index.d.ts"
    ],
    "engines": {
        "node": ">=6"
    },
    "scripts": {
        "lint": "eslint .",
        "test": "yarn lint && ava"
    },
    "keywords": [
        "filter",
        "prefix"
    ],
    "dependencies": {
        "remove-predicate": "^1.0.0"
    },
    "devDependencies": {
        "ava": "^3.0.0",
        "eslint": "^6.5.1",
        "eslint-config-richienb": "^0.2.0"
    },
    "eslintConfig": {
        "extends": "richienb/node"
    }
}
